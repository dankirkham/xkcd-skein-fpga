sudo: required
before_install:
  - sudo apt-get update
  - sudo apt-get install verilator python3.5

language: cpp

compiler:
  - gcc

install:
  - cd tools/xsf_asm && sudo python3.5 setup.py install

script:
  - cd src && make
